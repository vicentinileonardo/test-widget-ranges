apiVersion: widgets.templates.krateo.io/v1beta1
kind: Markdown
metadata:
  name: demo-sidebar-disclaimer
  namespace: demo-system
spec:
  widgetData:
    markdown: >
      **The branch list above and the detail panels on the right are updated
      every 3 minutes by default (aligned with the CDC reconciliation loop,
      env var `COMPOSITION_CONTROLLER_RESYNC_INTERVAL`).**
      