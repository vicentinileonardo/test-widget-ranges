{{- $cm := lookup "v1" "ConfigMap" "demo-system" .Release.Name -}}
{{- if not $cm }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: demo-system
data:
  items: "[]"
{{- end }}
