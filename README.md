The only fragility is the timing gap between ConfigMap content (Helm render time) and live RESTAction response (widget render time) — which we've mitigated with the // "N/A" null guards.

For example
N/A are used if the column on the right (panels) contains more elements than the left.